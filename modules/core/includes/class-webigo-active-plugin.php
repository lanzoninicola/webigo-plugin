<?php
/*
private $pods_plugin = 'pods/init.php';

private function list_active_plugins()
        {
            $this->activated_plugins = apply_filters('active_plugins', get_option('active_plugins'));
        }

        private function is_pod_plugin_activated()
        {
            $this->is_pod_plugin_active = in_array($this->pods_plugin, $this->activated_plugins);
        }
        */